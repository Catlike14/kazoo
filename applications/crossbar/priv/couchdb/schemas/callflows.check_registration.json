{
    "$schema": "http://json-schema.org/draft-04/schema#",
    "_id": "callflows.check_registration",
    "description": "Validator for the check_registration callflow data object",
    "properties": {
        "skip_module": {
            "description": "When set to true this callflow action is skipped, advancing to the wildcard branch (if any)",
            "type": "boolean"
        }
    },
    "type": "object"
}
